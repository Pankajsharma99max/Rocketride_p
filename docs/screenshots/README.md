This folder holds the images referenced by the main [README.md](../../README.md#screenshots).

Expected files:

| File | What it should show |
|---|---|
| `landing-hero.png` | The landing page's dark hero section (headline, eyebrow badge, interactive 3D scene) |
| `app-workspace.png` | The main app workspace — sidebar (upload/library) plus the chat panel |
| `rocketride-pipeline-design.png` | `ingestion.pipe` open in the RocketRide VS Code extension's visual editor (Design tab) |
| `rocketride-pipeline-status.png` | The same pipeline running, on the Status tab, with live performance metrics |

Add real screenshots here with these exact filenames and the README will pick them up automatically — no other changes needed.
