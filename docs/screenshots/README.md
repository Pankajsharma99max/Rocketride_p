This folder holds the images referenced by the main [README.md](../../README.md#screenshots).

| File | What it should show | Status |
|---|---|---|
| `landing-hero.png` | The landing page's dark hero section (headline, eyebrow badge, interactive 3D scene) | Included |
| `app-workspace.png` | The main app workspace — sidebar (upload/library) plus the chat panel | Included |
| `rocketride-pipeline-design.png` | `ingestion.pipe` open in the RocketRide VS Code extension's visual editor (Design tab) | Needed |
| `rocketride-pipeline-status.png` | The same pipeline running, on the Status tab, with live performance metrics | Needed |

The first two were captured directly from the running app. The last two need to come from a local VS Code session with the RocketRide extension installed and a pipeline open — add them here with these exact filenames and the README will pick them up automatically, no other changes needed.
